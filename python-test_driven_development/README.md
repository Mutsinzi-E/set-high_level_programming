# Python - Test-driven development

This directory contains Python exercises focused on test-driven development.

## Task 0: Integers Addition

The `0-add_integer.py` file contains the `add_integer()` function, which validates two values, converts floats to integers, and returns their sum.

Tests are provided in `tests/0-add_integer.txt`.
